import { PasswordShowPipe } from './password-show.pipe';

describe('PasswordShowPipe', () => {
  it('create an instance', () => {
    const pipe = new PasswordShowPipe();
    expect(pipe).toBeTruthy();
  });
});
